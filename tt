I'm working on a Vue 3 application using Pinia, Vue Router, and Tailwind CSS. I need help implementing new sections following our established CRUD pattern and component structure. Here are the details:

## Application Structure
- Each section follows a modal-driven CRUD pattern
- Each section has 5 core files:
  - IndexPage.vue (main listing with table)
  - NewItem.vue (modal for adding new items)
  - EditItem.vue (modal for editing existing items)
  - ViewItem.vue (modal for viewing item details)
  - ConfirmDeleteItem.vue (modal for confirming deletions)
- Each section has a corresponding Pinia store for state management

## Component Architecture
- All modals are controlled from the Index page, not via routes
- Shared reusable components:
  - ButtonBase (primary/secondary/add variants)
  - InputField (for form inputs)
  - SelectField (for dropdowns)
  - ViewField (for displaying data in view modals)
  - ViewModal (base modal for view screens)
  - Modal (base modal for other types)
  - TableComponent (for data listing with actions)
  - PageHeader (for page titles, search, and action buttons)

## Example Implementation
I need to implement the [Premium] section with the following fields:
- [Policy NO]
- [Payment Option]
- [Reason]
- [Amount]

Can you provide the implementation for all five Vue components and the Pinia store following the same pattern as our existing pages? Please ensure consistent use of props, events, and styling.

Here's how data flows in our application:
1. Index page fetches data from store and displays in TableComponent
2. "Add" button opens NewItem modal
3. Form submissions in modals call store actions
4. After CRUD operations, the store is updated and the table refreshes
5. All modals use v-model:show for visibility control
6. All form inputs use our shared InputField and SelectField components